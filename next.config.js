/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep config minimal & Vercel-friendly.
  // (TypeScript configs like next.config.ts can cause "Unexpected token" build errors in some environments.)
  reactStrictMode: true,
};

module.exports = nextConfig;
